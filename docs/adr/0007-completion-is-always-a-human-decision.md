# Completion is always a human decision

No external signal marks an Item done — not a merged pull request, a closed ticket, green CI, or an agent reporting success. Those are evidence the user weighs; treating them as decisions would quietly close work whose actual purpose the system cannot see. The same restraint extends to destruction: archiving never removes checkouts, branches or Panes, and marking an Item done never stops a running agent.
