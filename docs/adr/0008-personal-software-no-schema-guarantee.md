# v0 is personal software with no schema guarantee

This is built for one person on one Mac. The database schema may break between versions with no migration path, there is no signing, updater or distribution story, and backwards compatibility is explicitly not a goal — which is what licenses the speed the rest of the design assumes. Revisit if anyone else ever installs it.
